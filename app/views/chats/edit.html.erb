<br>
<h1 class='is-size-2' style="margin-left:20px">Editar chat</h1>
<br>
<p>Recuerda, solo puedes editar el chat completo, no un mensaje particular</p>
<br>
<div style="margin-left:20px">
<%= form_with(model: @chat, method: :patch, url: chats_update_path, local: true) do |form| %>
    <%- if @chat.errors.any? %>
        <div class="notification is-danger" style="width: 30%">
            Algo ha salido mal!
        </div>
    <% end %>
    <div class="field">
        <label class="label is-size-4 has-text-weight-light">Identificador Chat</label>
        <div class="control" style="padding-right: 70%">
            <%= form.text_field :donator, class: "input" %>
        </div>
    </div>
    <%- if @chat.errors[:donator].any?%>
        <div class= "help is-danger">
            <%= @chat.errors[:donator].first %>
        </div>
    <% end %>
    <br>
        <div class="field">
        <label class="label is-size-4 has-text-weight-light">Contenido Chat</label>
        <div class="control" style="padding-right: 70%">
            <%= form.text_field :chat, class: "input" %>
        </div>
    </div>
    <%- if @chat.errors[:chat].any?%>
        <div class= "help is-danger">
            <%= @chat.errors[:chat].first %>
        </div>
    <% end %>
    <br>

    <div class="control">
        <div class="action">
            <%= form.submit "Guardar", class: "button is-link" %>
        </div>
    </div>
<% end %>
</div>
<button class="button is-rounded" style="margin-top: 20px; margin-left: 20px; margin-bottom: 30px"><%= link_to 'Volver',chats_show_path(@chat.id) if @chat %></button>
<button class="button is-rounded" style="margin-top: 20px; margin-left: 20px; margin-bottom: 30px"><%= link_to 'Otros chats', chats_index_path %></button>